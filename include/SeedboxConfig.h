#pragma once

// Local configuration overrides live here. The defaults ship with QUIET_MODE=1.
// Define macros before including any other SeedBox headers.
// Example:
//   #undef QUIET_MODE
//   #define QUIET_MODE 0
// so you can persist data during experiments.
