# SB-12 — Reversible performance gestures

`SeedBranch` supplies the minimal performance grammar: fork the current
executable seed, mutate its branch, then return exactly to the captured
ancestor. It is a control-side object, so it adds no real-time engine history.
Physical gesture binding remains intentionally deferred until a hardware pass.
